steps:
- task: CopyFiles@2
  displayName: "Copy nuget to artifacts"
  inputs:
    SourceFolder: '**/NugetProject/bin/$(buildConfiguration)'
    Contents: 'ComboPatterns.$(packageVersion).nupkg'
    TargetFolder: '$(Build.ArtifactStagingDirectory)'
