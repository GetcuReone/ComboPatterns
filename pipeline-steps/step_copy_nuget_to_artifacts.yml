steps:
- task: CopyFiles@2
  displayName: "Copy nuget to artifacts"
  inputs:
    SourceFolder: '$(Build.Repository.LocalPath)/$(Build.Repository.Name)/NugetProject/bin/$(buildConfiguration)'
    Contents: '$(PackageId).$(packageVersion).nupkg'
    TargetFolder: '$(Build.ArtifactStagingDirectory)'
