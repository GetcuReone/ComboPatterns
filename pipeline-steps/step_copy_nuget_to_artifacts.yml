steps:
- task: CopyFiles@2
  displayName: "Copy nuget to artifacts"
  inputs:
    SourceFolder: '$(Build.Repository.LocalPath)/$(solutionName)/NugetProject/bin/$(buildConfiguration)'
    Contents: '$(packageName).$(packageVersion).nupkg'
    TargetFolder: '$(Build.ArtifactStagingDirectory)'
